apiVersion: v1
kind: Service
metadata:
  name: matching-management-context
  labels:
    app: matching-management-context
spec:
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: matching-management-context